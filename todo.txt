Edge cases{
	refresh causes waitlist to be emptied: do one of two things ->

	1) Store waitlist in localstorage(fails if machine shuts down in between)

	2) Store waitlist as table in backend:
	
	Approach:
		Can store waitlist at the localstorage for now.

	Then implement a filter in customer order items screen.
}

CSS work has begun.

Note: bill generation has been handled keeping a actual restaurant system in mind. The json generated from the bill will be sent to the bill printing machine/service which will then process the json and print out the bill. For now we are just printing the json in the console.
